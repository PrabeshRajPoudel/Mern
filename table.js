// function table(index, first, last) {

//     for (let i = first; i <= last; i++) {
//         console.log(`${index} * ${i} =  ${index * i}`);
//     }
//     console.log("\n")
// }
// table(3, 5, 10)
// table(4, 2, 6)
// table(5, 7, 10)
// table(6, 5, 20)
let table = (index, first, last) => {

    for (let i = first; i <= last; i++) {
        console.log(`${index} * ${i} =  ${index * i}`);
    }
    console.log("\n")
}
table(3, 5, 10)
table(4, 2, 6)
table(5, 7, 10)
table(6, 5, 20)